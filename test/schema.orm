datasource db {
  provider = "mysql"
  url      = "mysql://avnadmin:AVNS_WfMxzdmerTkhHKLLpcV@sharetext-codepad.f.aivencloud.com:16805/defaultdb?ssl=true"
  ssl      = "./test/ca.pem"
}

model User {
  id    Int     @id @default(autoincrement())
  name  String
  email String  @unique
}

model Post {
  id       Int    @id @default(autoincrement())
  title    String
  content  String?
  userId   Int    @references(User.id) @onDelete(cascade)
}